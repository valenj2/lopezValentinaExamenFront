import { useState } from "react";
import Form from "./Componentes/Card";

function App() {
  const [count, setCount] = useState(0);

  return;
  <div>
    <h1>Form</h1>
    <Form />
  </div>;
}

export default App;
